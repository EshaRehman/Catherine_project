/**
 * Locked kiosk output: portrait 4:5 (e.g. 1080×1350) — social + print aligned.
 * Width : height = 4 : 5
 */
export const OUTPUT_ASPECT_W = 4;
export const OUTPUT_ASPECT_H = 5;
export const OUTPUT_ASPECT_RATIO = OUTPUT_ASPECT_W / OUTPUT_ASPECT_H;

export const OUTPUT_WIDTH = 1080;
export const OUTPUT_HEIGHT = 1350;

/** Half-res mock / quick previews (same ratio) */
export const OUTPUT_PREVIEW_WIDTH = 540;
export const OUTPUT_PREVIEW_HEIGHT = 675;

/** Template editor “Generate preview” canvas */
export const OUTPUT_EDITOR_MOCK_WIDTH = 360;
export const OUTPUT_EDITOR_MOCK_HEIGHT = 450;
