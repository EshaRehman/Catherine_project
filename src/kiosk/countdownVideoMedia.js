/**
 * In-frame “1 2 3” clip after shutter tap, before capture + processing.
 * Place `IMG_0766.MOV` in the project root (same as other kiosk videos).
 */
import countdownVideoUrl from '../../IMG_0766.MOV';

export default countdownVideoUrl;
