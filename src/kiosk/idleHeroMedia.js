/**
 * Idle-screen hero clip. Webpack treats `.mp4` and `.mov` as assets; Chromium plays both.
 * Change this import to your file (either extension is fine — only one file is bundled).
 */
import heroUrl from '../../IMG_0759.MOV';

export default heroUrl;
