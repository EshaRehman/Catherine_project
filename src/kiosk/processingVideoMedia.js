/**
 * Processing / “creating your transformation” clip inside the 4:5 frame.
 * Point this import at your `.mov` or `.mp4` file in the project root.
 */
import processingVideoUrl from '../../IMG_0769.MOV';

export default processingVideoUrl;
